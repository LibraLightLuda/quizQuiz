# NumberCal 구현 명세서

> 문서 목적: 이 문서만으로 1명의 개발자 또는 후속 AI 개발 세션이 정적 PWA 전체를 구현할 수 있도록 제품·UX·도메인 규칙·상태 전환·배포 조건을 결정한다. 이번 문서는 구현 명세이며 실제 소스 코드는 포함하지 않는다.

## 0. 결정 요약

| 항목 | 결정 |
|---|---|
| 앱 형태 | 서버 없는 모바일 우선 단일 페이지 PWA |
| 기술 스택 | React + TypeScript + Vite + `vite-plugin-pwa` + 일반 CSS |
| 배포 | GitHub Actions로 `dist`를 GitHub Pages에 배포 |
| 라우팅 | URL 라우터 없이 앱 상태로 화면 전환 |
| 기본 세션 | 5문제 작은 모험, 선택형 15문제 긴 모험, `쉬움` 난이도 |
| 문제 형식 | 쉬움·보통·어려움은 선택형, 도전은 직접 입력형 |
| 저장 | 버전이 붙은 `localStorage`, 최근 완료 세션 최대 20개 |
| 오프라인 | 앱 셸과 학습 데이터를 precache. 외부 CDN·서버·폰트 없음 |
| 음성 | 브라우저 Web Speech API 우선, 실패 시 읽기 문제로 안전하게 대체 |
| 핵심 안전 규칙 | 문제당 답안 확정은 정확히 한 번만 허용하고 타이머·연타 이벤트는 이후 무시 |

---

## 1. 제품 목표

유치원생부터 초등학생까지 혼자 사용할 수 있는 수학·한국어·영어 선택형 학습 앱을 만든다. 밝고 친근하지만 지나치게 유아틱하지 않은 현대적 교육 앱을 지향한다.

필수 제품 조건은 다음과 같다.

- iPhone, iPad, Android 스마트폰·태블릿을 우선 지원하고 PC에서도 사용할 수 있어야 한다.
- 회원가입, 서버 DB, 광고, 분석 SDK, 유료 API를 사용하지 않는다.
- 정적 파일만으로 동작하고 GitHub Pages의 저장소 하위 경로에서도 배포되어야 한다.
- 최초 온라인 로드 및 캐시 설치가 끝난 뒤에는 학습, 설정, 결과 저장이 오프라인에서 동작해야 한다.
- TTS처럼 운영체제·브라우저 기능에 의존하는 기능이 실패해도 나머지 앱은 계속 동작해야 한다.
- 수학은 문제마다 30초 제한과 남은 시간을 표시한다. 한국어·영어는 시간 제한 없이 아이가 충분히 듣고 생각할 수 있게 한다.

비목표는 계정·프로필, 기기간 동기화, 순위표, 결제, 원격 콘텐츠 관리, 손글씨 인식, 서버 기반 음성 파일 생성이다.

## 2. 주요 사용자

### 2.1 주 사용자

- 글을 막 읽기 시작한 유치원생과 초등학생
- 마우스보다 손가락 터치를 주로 사용하며 긴 설명을 읽기 어려울 수 있는 사용자
- 한국어 UI를 사용하며, 영어는 기초 단어 학습 대상으로 접하는 사용자

### 2.2 보조 사용자

- 세션 길이·난이도·소리·애니메이션을 설정하는 보호자 또는 교사
- 최근 학습 결과를 간단히 확인하는 보호자

### 2.3 기본 UX 원칙

- 한 화면의 주요 행동은 하나로 제한한다.
- 선택지는 최소 56×56 CSS px, 일반 버튼은 최소 48×48 CSS px의 터치 영역을 갖는다.
- 첫 실행 기본값은 `쉬움 / 작은 모험 5문제 / 효과음 켜짐 / TTS 켜짐 / 애니메이션 켜짐`이다. 수학만 문제마다 30초를 적용한다.
- 부정적 점수 표현, 실패 연속 횟수, 순위, 벌점은 보여주지 않는다.
- 설정은 숨기지 않되 문제 풀이의 주 시선 흐름을 방해하지 않는 톱니바퀴 버튼으로 제공한다.

## 3. 전체 UX 흐름

기본 흐름은 아래와 같다.

1. 홈에서 `수학`, `한국어`, `영어` 중 하나를 선택한다.
2. 과목별 학습 모드를 선택한다.
3. 난이도를 선택한다.
4. `작은 모험 5문제` 또는 `긴 모험 15문제`를 고른다. 언어 학습은 동물·음식·자연 테마도 고른 뒤 `시작`을 누른다.
5. 앱이 문제를 만들고 진행도와 함께 표시한다.
6. 아이가 보기를 선택한다. 도전 난이도에서는 숫자 또는 글자를 직접 입력한다.
7. 정답 또는 오답 피드백을 짧게 보여준다.
8. 자동으로 다음 문제로 이동한다.
9. 마지막 문제 뒤에는 결과 화면을 보여준다.
10. `한 번 더`는 같은 설정으로 새 세션, `처음으로`는 홈으로 이동한다.

최근 선택값은 기억한다. 같은 과목에 다시 들어왔을 때 마지막 모드와 난이도를 기본 선택하되, 시작 전에는 항상 사람이 읽을 수 있는 요약을 보여준다.

문제 풀이 중 뒤로 가기를 누르면 즉시 나가지 않고 `계속 풀기`와 `여기까지 하고 나가기`가 있는 확인 대화상자를 연다. 중도 종료 세션은 최근 학습 기록에 저장하지 않는다.

## 4. 화면 구조

### 4.1 홈 화면

- 앱 이름과 짧은 안내
- 수학·한국어·영어 대형 과목 카드 3개
- 설정 버튼
- 최근 완료 세션이 있으면 마지막 결과 한 줄만 표시. 기록이 없어도 빈 상태가 어색하지 않아야 한다.

### 4.2 모드 선택 화면

- 선택한 과목 제목과 뒤로 가기
- 과목별 모드 카드
- 모드별 한 줄 예시

### 4.3 난이도·세션 설정 화면

- 4단계 난이도 카드. 학년 숫자보다 친근한 이름을 우선 표시한다.
- `작은 모험 5문제`를 기본으로 보여주고 `긴 모험 15문제`를 보조 선택으로 제공한다. 언어는 `천천히 해도 괜찮아요`, 수학은 `문제마다 30초`로 안내한다.
- 현재 선택 요약과 큰 `시작` 버튼

### 4.4 문제 화면

- 상단: 나가기, 과목명, `현재 번호 / 전체 문제 수`
- 진행 막대
- 수학에서는 남은 시간을 큰 숫자와 시각 막대로 표시한다. 언어에서는 타이머 영역을 표시하지 않는다.
- 중앙: 문제·힌트 또는 듣기 버튼
- 하단: 보기 버튼 3~4개 또는 도전 난이도의 직접 입력창
- 선택 뒤에는 같은 영역에 피드백을 겹치되 문제와 정답을 완전히 가리지 않는다.
- 별도의 `다음` 버튼은 기본적으로 두지 않고 짧은 피드백 뒤 자동 진행한다. 애니메이션 감소 설정에서도 전환 지연은 유지한다.

### 4.5 결과 화면

- 별 또는 체크 아이콘, `8 / 10`, `8문제를 맞혔어요!`처럼 즉시 이해되는 요약
- 수학은 정답 수·다시 연습할 문제 수·평균 응답 시간을, 언어는 오늘 발견한 단어·다시 만날 단어·이야기 스티커를 보여준다.
- 수학에서 시간 초과가 있었을 때만 작은 보조 항목으로 표시한다.
- `한 번 더 만나기`, `다른 모험 고르기`, `처음으로`
- 최근 기록 목록 전체를 이 화면에 넣지 않는다.

### 4.6 설정 화면 또는 모달

- 효과음 켜기/끄기
- TTS 켜기/끄기와 0.75·0.85·0.95 읽기 속도 선택
- 큰 시각 효과·애니메이션 켜기/끄기
- 최근 학습 기록 지우기. 누르면 확인을 한 번 받는다.
- 기기 설정의 동작 줄이기가 활성화되어 있으면 그 사실을 짧게 알린다.

## 5. 학습 모드

| 과목 | 모드 ID | 사용자 표시명 | 설명 |
|---|---|---|---|
| 수학 | `math-add` | 덧셈 | 두 수 또는 고난도에서 세 수를 더한다. |
| 수학 | `math-subtract` | 뺄셈 | 모든 단계에서 결과가 0 이상인 문제를 푼다. |
| 수학 | `math-multiply` | 곱셈 | 쉬운 구구단에서 두 자리 수×한 자리 수까지 확장한다. |
| 수학 | `math-mixed` | 사칙연산 | 덧셈·뺄셈·곱셈을 섞어 출제한다. |
| 한국어 | `ko-fill` | 글자 채우기 | 힌트를 보고 빠진 완성형 음절 또는 음절 묶음을 고른다. |
| 한국어 | `ko-listen` | 듣고 고르기 | 화면에 목표 단어를 쓰지 않고 한국어 음성을 듣고 단어를 고른다. |
| 영어 | `en-fill` | 철자 채우기 | 한국어 뜻·힌트를 보고 빠진 영문자 묶음을 고른다. |
| 영어 | `en-listen` | 듣고 고르기 | 영어 음성을 듣고 단어를 고른다. |
| 한국어 | `ko-adventure` | 말놀이 탐험 | 소리 찾기·낱말 조립·그림 연결·문장 완성을 공통 활동 화면에서 진행한다. |
| 영어 | `en-adventure` | Word Quest | 소리·철자·그림·짧은 문장 활동을 공통 활동 화면에서 진행한다. |

과목 혼합 모드는 제공하지 않는다. 언어 학습은 아이가 고른 범위 안에서 단어별 숙련도를 사용한다. 5문제 작은 모험은 `환영 1 / 발견 2 / 다시 만나기 1 / 이야기 1`, 15문제 긴 모험은 `환영 3 / 발견 6 / 다시 만나기 3 / 이야기 3`의 목적별 덱으로 구성한다. 틀린 단어는 간격을 둔 뒤 다시 출제할 수 있고 결과 화면과 다음 세션에서도 복습 대상으로 표시한다. 사용자가 고른 학습 범위 자체는 자동으로 바꾸지 않는다.

## 6. 난이도 정의

모든 과목은 같은 4단계 ID를 사용하되 실제 문제 규칙은 과목별로 다르다. 학년 표기는 정확한 교육과정 대응을 뜻하지 않으므로 UI에서는 보조 설명으로만 쓴다.

| ID | 표시명 | 대략적 대상 | 보기 수 | 공통 성격 |
|---|---|---:|---:|---|
| `easy` | 쉬움 | 초1~2 | 3 | 짧은 문제, 익숙한 단어·수 |
| `normal` | 보통 | 초2~3 | 4 | 표준 문제, 근접 오답 포함 |
| `hard` | 어려움 | 초3~4 | 4 | 자릿수·빈칸 증가, 비슷한 오답 |
| `challenge` | 도전 | 초4~6 | 직접 입력 | 가장 큰 범위, 숫자 또는 글자를 직접 입력 |

기본 난이도는 `easy`다. 난이도 설명에는 예시 하나를 함께 보여줘 선택 결과를 예측할 수 있게 한다.

### 6.1 수학 난이도표

| 난이도 | 덧셈 | 뺄셈 | 곱셈 |
|---|---|---|---|
| 쉬움 | 0~20 두 수, 합 ≤ 30 | 0~30 두 수, 차 ≥ 0, 받아내림은 최대 1회 | 2·3·4·5·10단 × 1~9 |
| 보통 | 10~99 두 수 | 10~99 두 수, 차 ≥ 0, 받아내림 허용 | 2~9 × 2~9 |
| 어려움 | 10~299 두 수, 일부 받아올림 2회 | 10~299 두 수, 차 ≥ 0 | 2~20 × 2~9 |
| 도전 | 100~999 두 수 또는 1~99 세 수 | 100~999 두 수, 차 ≥ 0 | 10~99 × 2~9 |

음수는 초등 대상 범위를 벗어나므로 어느 단계에서도 만들지 않는다.

### 6.2 한국어 난이도표

| 난이도 | 단어 길이 | 숨기는 범위 | 어휘 | 오답 성격 |
|---|---:|---:|---|---|
| 쉬움 | 2~3음절 | 1음절 | 집·학교·놀이터의 익숙한 단어 | 같은 길이의 쉬운 음절 |
| 보통 | 2~4음절 | 연속 1~2음절 | 기초 교과·자연·생활 | 한 음절이 비슷한 오답 포함 |
| 어려움 | 3~5음절 | 연속 2음절 중심 | 조금 덜 빈번한 초등 어휘 | 유사 발음·유사 철자 묶음 |
| 도전 | 3~6음절 | 연속 2~3음절 | 초등 고학년 생활·교과 어휘 | 실제로 헷갈리기 쉬운 묶음 |

### 6.3 영어 난이도표

| 난이도 | 단어 길이 | 숨기는 범위 | 어휘 | 오답 성격 |
|---|---:|---:|---|---|
| 쉬움 | 3~5자 | 1자 | 유치원·초등 기초 단어 | 모음 또는 흔한 자음 오답 |
| 보통 | 4~7자 | 연속 1~2자 | 기초 생활 단어 | 발음·철자가 가까운 오답 |
| 어려움 | 5~9자 | 연속 2자 중심 | 철자 묶음이 있는 단어 | `ch/sh/th/ph` 같은 유사 묶음 |
| 도전 | 6~12자 | 연속 2~3자 | 초등 고학년 기초 어휘 | 접두·중간·접미 철자 유사 오답 |

## 7. 수학 문제 생성 규칙

### 7.1 생성 절차

1. 현재 연산과 난이도의 피연산자 규칙을 가져온다.
2. 난수로 피연산자를 만든다.
3. 뺄셈은 큰 수를 앞에 배치하여 정답이 0 이상이 되게 한다.
4. 결과와 문제 서명(signature)을 계산한다.
5. 최근 문제와 중복되는지 검사한다.
6. 정답 근처의 교육적으로 그럴듯한 오답 후보를 만든다.
7. 중복·정답·허용 범위 밖 값을 제거하고 필요한 수만큼 고른다.
8. 정답을 합쳐 Fisher–Yates 방식으로 보기를 섞는다.
9. 모든 불변식을 검사한 뒤 문제를 반환한다.

### 7.2 문제 서명과 반복 방지

- 서명은 `mode:normalizedOperands` 형태다. 교환법칙이 적용되는 덧셈·곱셈은 피연산자를 정렬하여 `3+4`와 `4+3`을 같은 문제로 본다.
- 현재 세션의 최근 8개 서명에는 같은 문제가 나오지 않게 한다.
- 동일 정답 값이 3문제 연속 나오지 않게 한다.
- 생성 시 최대 30회 재시도한다. 30회 실패하면 최근 중복 제한을 8개에서 4개로만 완화한다. 유효성·정답 유일성 규칙은 절대 완화하지 않는다.
- 테스트에서는 seed가 있는 난수원을 주입할 수 있게 하고, 실제 앱에서는 `crypto.getRandomValues` 기반 난수를 사용한다.

### 7.3 오답 후보 규칙

완전 무작위 숫자보다 아래 후보를 우선순위로 사용한다.

- 정답의 `±1`, `±2`
- 두 자리 이상은 `±10`, 세 자리 이상은 `±100`
- 한쪽 피연산자를 1 크게 또는 작게 계산한 값
- 덧셈·뺄셈의 받아올림/받아내림을 한 자리에서 놓친 값
- 곱셈의 인접 구구단 값: `a×(b-1)`, `a×(b+1)`, `(a-1)×b`, `(a+1)×b`
- 연산을 혼동한 값은 결과가 너무 멀지 않고 음수가 아닐 때만 낮은 우선순위로 사용

보통 이상에서는 최소 절반의 오답이 정답과 가까운 후보여야 한다. 후보가 부족하면 정답 주변 값을 넓혀 채우되 정답과 중복될 수 없다.

### 7.4 세 수 덧셈

`challenge`에서만 전체 덧셈 문제의 최대 25%를 세 수 문제로 만든다. 괄호나 연산 우선순위를 묻지 않고 `a + b + c` 형식만 사용한다. 다른 연산의 복합식은 1차 범위에서 제외한다.

## 8. 한국어 문제 생성 규칙

### 8.1 데이터 원칙

- 단어 데이터는 UI·생성 코드와 분리된 정적 데이터 파일로 둔다.
- 최소 5개 카테고리(동물, 음식, 학교, 가족·사람, 자연·생활)를 제공한다.
- 각 난이도에 채우기 가능한 단어를 최소 30개, 전체 120개 이상을 목표로 한다.
- 한 항목에는 고유 ID, 완성 단어, 난이도, 카테고리, 아이가 이해할 짧은 힌트, 선택적 이모지, 허용 마스킹 구간, 선택적 수동 오답 묶음을 둔다.
- 텍스트는 NFC로 정규화하고 `Array.from` 기준 완성형 한글 음절 단위로 처리한다. 자모를 분리해 묻지 않는다.

### 8.2 글자 채우기

1. 난이도와 길이 조건에 맞는 단어를 고른다.
2. 데이터에 지정된 허용 구간 중 하나를 고르거나 난이도표에 맞는 연속 구간을 고른다.
3. 빠진 음절 수만큼 `□`를 표시한다. 여러 음절 정답도 보기 버튼 하나에 한 묶음으로 표시한다.
4. 단어를 직접 답으로 말하지 않는 이모지·짧은 힌트를 함께 표시한다. 예: `🍎 먹는 과일 · 사□`.
5. 같은 음절 수의 수동 오답을 우선 사용하고, 부족하면 같은 난이도·카테고리 데이터의 음절 묶음에서 가져온다.

`사□ [과] [자]`처럼 두 결과가 모두 실제 단어인 경우에도 힌트가 목표를 분명히 해야 한다. 힌트 없이 둘 이상의 자연스러운 단어가 완성되는 문제는 생성하지 않는다.

### 8.3 듣고 고르기

- 목표 단어를 화면의 제목, 접근성 이름, DOM의 보이는 텍스트에 문제 풀이 중 노출하지 않는다.
- 보기는 목표 단어 1개와 같은 난이도의 서로 다른 단어들로 구성한다.
- 우선 같은 카테고리에서 오답을 뽑고 부족하면 인접 카테고리로 넓힌다.
- 어려움 이상에서는 한두 음절이 비슷한 단어를 일부 포함할 수 있다.
- 음성이 실패하여 현재 문제를 읽기 문제로 바꾸어야 할 때는 해당 문제를 기록하지 않고 새 문제 ID로 교체한다.

## 9. 영어 문제 생성 규칙

### 9.1 데이터 원칙

- 데이터 항목은 고유 ID, 소문자 표제어, 한국어 뜻·힌트, 난이도, 카테고리, TTS 언어, 허용 마스킹 구간, 선택적 오답 묶음을 가진다.
- 표제어는 기본적으로 `a-z` 소문자 단어 하나로 제한한다. 하이픈, 공백, 고유명사는 1차 릴리스에서 제외한다.
- 전체 120개 이상, 난이도별 최소 30개를 목표로 한다.
- UI에서는 문제 표기를 소문자로 통일한다. TTS에는 원래 표제어를 전달한다.

### 9.2 철자 채우기

1. 난이도와 길이에 맞는 단어를 고른다.
2. 단어의 처음·중간·끝이 고르게 출제되도록 허용 구간 중 하나를 선택한다.
3. 숨긴 글자 수만큼 `□`를 표시하고 한국어 뜻 또는 답을 직접 포함하지 않는 쉬운 힌트를 함께 보여준다. 예: `고양이 · c□t`.
4. 오답은 수동 오답을 먼저 사용하고, 같은 길이의 모음·자음·철자 묶음으로 보충한다.
5. 오답을 삽입한 결과가 힌트에 맞는 다른 정답이 되거나 데이터 목록의 흔한 단어와 충돌하면 그 오답을 버린다.

영어는 철자 패턴만으로 `cat/cot`처럼 여러 실제 단어가 만들어질 수 있으므로 한국어 뜻·힌트 표시를 필수로 한다.

### 9.3 듣고 고르기

- 목표 단어를 `en-US` 우선으로 읽고, 해당 음성이 없으면 다른 `en-*` 음성을 사용한다.
- 쉬움은 소리와 철자가 크게 다른 보기로 시작한다.
- 보통 이상은 같은 카테고리 또는 비슷한 길이의 단어를 섞되 동음이의·동음어는 같은 문제에 넣지 않는다.
- 대소문자만 다른 보기는 중복으로 본다.

## 10. 선택지 생성 규칙

모든 과목에 아래 불변식을 적용한다.

- 보기 개수는 `쉬움 3, 보통·어려움 4`다. 도전은 보기를 표시하지 않고 정답을 직접 입력한다.
- 정답 보기는 정확히 하나다.
- 표시 문자열을 과목별로 정규화한 뒤 모든 보기가 서로 달라야 한다. 숫자는 수치 값, 한국어는 NFC 문자열, 영어는 소문자 문자열로 비교한다.
- 빈 문자열, 잘린 문자열, `NaN`, 범위 밖 숫자는 허용하지 않는다.
- 정답 위치는 Fisher–Yates로 섞는다.
- 정답이 같은 위치에 3문제 연속 놓이지 않도록 한 번 재섞는다. 그래도 조건을 만족하지 못하면 정답 유일성을 우선하고 그대로 사용한다.
- 보기 순서는 문제 표시 이후 절대 바꾸지 않는다.
- 보기 버튼의 내부 ID와 표시 문자열을 분리한다. 답 판정은 표시 문자열이나 배열 위치가 아니라 `optionId === correctOptionId`로 한다.
- 문제 생성 종료 시 개발 빌드와 테스트에서 불변식 검사를 실행한다. 운영 빌드에서도 실패한 문제는 표시하지 않고 재생성한다.

언어 오답 후보가 부족하면 순서대로 `수동 오답 → 같은 카테고리·난이도 → 같은 난이도 → 인접 난이도`로 넓힌다. 그래도 부족하면 검증된 비상 문제 목록을 사용한다. 보기 수나 정답 유일성은 낮추지 않는다.

## 11. TTS 설계

### 11.1 기본 동작

- 브라우저의 `window.speechSynthesis`와 `SpeechSynthesisUtterance`만 사용한다.
- 한국어는 `ko-KR`, 영어는 `en-US`를 우선한다. 정확한 locale 음성, 같은 언어 계열 음성, 브라우저 기본 음성 순으로 선택한다.
- 자동 읽기와 일반 다시 듣기는 저장된 0.75·0.85·0.95 속도를 사용한다. `느리게 다시 듣기`는 설정값과 관계없이 0.75를 사용한다.
- `speechSynthesis.getVoices()`가 처음에 비어 있을 수 있으므로 `voiceschanged`를 구독하고 목록을 갱신한다.
- 기본 속도는 한국어 `0.9`, 영어 `0.85`, pitch `1.0`, volume `0.9`다.
- 목표 단어만 한 번 읽는다. 문제 설명이나 보기 전체를 TTS로 자동 낭독하지 않는다.
- 듣기 문제에는 최소 64×64 CSS px의 `다시 듣기` 버튼과 `재생 중` 상태를 둔다.
- 새 문제, 홈 이동, 설정에서 TTS 끄기, 페이지 숨김 시 현재 발화를 `cancel()`한다. 이전 문제의 `end/error` 이벤트는 question ID가 다르면 무시한다.

### 11.2 자동 재생과 사용자 인터랙션

- 세션의 `시작` 클릭에서 TTS 사용 가능 여부를 확인하고 가능한 경우 첫 문제 발화를 시작한다.
- 다음 문제 자동 발화는 브라우저 정책상 실패할 수 있다. 실패해도 `다시 듣기` 버튼은 항상 남기며, 사용자의 직접 탭으로 다시 시도한다.
- Web Speech API가 없으면 설정·모드 선택 화면에서 듣기 모드를 비활성화하고 `이 기기에서는 듣기를 사용할 수 없어요. 글자 문제를 이용해 주세요.`라고 안내한다.
- 발화 `error` 또는 무음으로 의심되는 실패가 연속 2회 발생하면 `읽기 문제로 바꾸기` 버튼을 제공한다. 전환 시 현재 문제는 점수에 포함하지 않고 같은 번호의 새 채우기 문제로 교체한다.
- 음성 목록이 비어 있다는 이유만으로 즉시 듣기를 막지 않는다. `lang`이 지정된 기본 utterance를 한 번 시도한다.

### 11.3 언어 학습과 TTS

- 언어 듣기 문제에는 제한 시간을 두지 않는다. 첫 자동 발화가 끝난 뒤에도 아이가 원하는 만큼 다시 들을 수 있다.
- `end`가 오지 않아도 답변 UI는 유지하고 다시 듣기 또는 읽기 문제 전환을 제공한다.
- 다시 듣기는 횟수 제한 없이 사용할 수 있다.
- TTS가 꺼진 상태에서는 듣기 모드를 시작할 수 없고 채우기 모드로 바꿀지 안내한다.
- 기기 TTS 음성이 오프라인에서 제공된다고 보장하지 않는다. 오프라인 발화 실패는 위 읽기 문제 대체 흐름으로 처리한다.

## 12. 타이머 및 상태 전환

### 12.1 과목별 시간 규칙

수학 문제의 제한 시간은 문제마다 30초다. 한국어·영어에는 deadline과 시간 초과 판정을 만들지 않는다. 정답 피드백은 500ms, 오답·수학 시간 초과 피드백은 650ms 동안 표시한다. 큰 애니메이션을 꺼도 피드백 지연은 유지한다.

### 12.2 단일 문제 상태 흐름

`GENERATING → PRESENTING → ANSWERING → FEEDBACK → ADVANCING`

- `GENERATING`: 이전 타이머와 TTS를 취소하고 새 question ID를 만든다. 보기는 비활성 상태다.
- `PRESENTING`: 문제를 DOM에 표시한다. 일반 문제는 즉시 `ANSWERING`으로 간다. 듣기 문제는 첫 TTS를 요청하고 종료 또는 4초 fallback 뒤 `ANSWERING`으로 간다.
- `ANSWERING`: 보기 입력을 받는 유일한 상태다. 이 상태에 들어간 시각을 기록하고 수학에서만 deadline을 기록한다.
- `FEEDBACK`: 선택 또는 시간 초과 결과가 확정된 상태다. 모든 보기를 비활성화하고 기록·피드백을 정확히 한 번 수행한다.
- `ADVANCING`: 피드백 타이머가 끝난 뒤 다음 번호를 결정한다. 남은 문제가 있으면 `GENERATING`, 없으면 `COMPLETED`로 간다.

세션 전체 흐름은 `SETUP → STARTING → ACTIVE → COMPLETED`이며, 나가기 확인 중에는 `PAUSED_CONFIRM`, 앱이 백그라운드로 가면 `PAUSED_HIDDEN`을 사용한다.

### 12.3 경쟁 조건·연타 처리

다음 규칙은 구현 필수 불변식이다.

1. 답 선택 이벤트와 수학 시간 초과 이벤트는 모두 현재 `sessionId`, `questionId`, `occurredAt`을 포함한다.
2. reducer 또는 동등한 단일 상태 전환 함수는 현재 상태가 `ANSWERING`이고 ID가 일치할 때만 최초 이벤트 하나를 승인한다.
3. 승인과 동시에 상태를 동기적으로 `FEEDBACK`으로 바꾸고 `resolvedAt`과 `resolution`을 기록한다. 클릭 핸들러도 즉시 로컬 입력 잠금을 걸어 같은 이벤트 루프의 연타를 막는다.
4. 승인 뒤 도착한 두 번째 클릭, 오래된 타이머, 오래된 TTS 콜백은 아무 효과 없이 무시한다.
5. 수학 선택 이벤트가 deadline 이하에 동기 처리되면 선택 결과를 인정한다. deadline을 넘긴 선택은 시간 초과로 처리한다. 언어는 deadline 검사를 하지 않는다. 같은 틱의 모호한 상황에서도 기록은 한 건만 생겨야 한다.
6. 결과 배열 추가, 점수 증가, 소리·애니메이션 시작은 `FEEDBACK` 진입 효과에서 question ID별 한 번만 실행한다. React Strict Mode의 개발 중 effect 재실행에도 중복되지 않아야 한다.
7. 타이머는 1초씩 감소시키는 카운터가 아니라 `performance.now()` 기반 deadline 차이로 계산한다. 화면 표시는 100~250ms 간격으로 갱신해도 판정은 실제 deadline으로 한다.
8. 피드백 전환 타이머도 question ID를 확인한다. 화면 이동·새 세션·unmount 시 모든 timeout을 정리한다.

### 12.4 페이지 숨김과 재개

- `visibilitychange`로 페이지가 숨겨지면 수학 제한 시간을 일시 정지하고 남은 시간을 저장한다. 언어는 현재 문제 상태만 보존한다.
- 돌아오면 `계속하기` 오버레이를 보여주고 사용자의 탭으로 재개한다. 듣기 문제는 이 탭에서 단어를 다시 읽은 뒤 재개할 수 있다.
- 숨김 중 발생한 타이머·TTS 콜백은 상태와 ID 검사로 무시한다.
- 일시 정지 시간은 평균 응답 시간에서 제외한다.

## 13. 정답 칭찬 피드백 시스템

### 13.1 문구

기본 문구 묶음은 `정답!`, `잘했어요!`, `대단해요!`, `멋져요!`, `최고예요!`, `한 문제 더!`다. 셔플 백 방식으로 한 묶음을 모두 사용하기 전까지 같은 문구를 반복하지 않고, 새 묶음의 첫 문구가 직전 문구와 같으면 한 번 교환한다.

### 13.2 시각 피드백

- 선택한 정답 버튼을 긍정 색상과 체크 아이콘으로 표시한다.
- 0.8초 내외의 작은 별 3~5개 또는 짧은 반짝임을 문제 카드 주변에서 보여준다.
- confetti는 화면 전체가 아니라 상단 30% 안에서 0.9초 이내, 최대 12조각만 사용한다.
- 같은 세션에서 큰 효과는 3문제당 최대 한 번만 보여주고 나머지는 체크·별 효과로 제한한다.
- 애니메이션 설정이 꺼졌거나 `prefers-reduced-motion: reduce`이면 움직임 없이 체크, 색상, 문구만 표시한다.

### 13.3 연속 정답

- 3회와 5회 연속 정답에서만 `⭐ 3문제 연속 성공!`처럼 작은 보너스 문구를 표시한다.
- 6회 이후에는 매 5회 단위로만 표시한다.
- 연속 기록이 끊겼다는 문구·소리·감소 애니메이션은 절대 보여주지 않는다.
- 연속 정답은 세션 안에서만 유지하고 장기 기록이나 경쟁 요소로 사용하지 않는다.

## 14. 오답 피드백

- 문구는 `다시 생각해볼까요?`, `괜찮아요!`, `다음 문제도 해봐요!`, `조금만 더 해봐요!` 중 반복을 피해서 고른다.
- 선택한 오답은 주황 계열 테두리와 `선택한 답` 아이콘으로 표시한다. 정답은 초록 계열과 체크로 함께 표시한다.
- 빨간 화면 흔들기, 경고음, X가 크게 튀는 효과, `틀렸어요!` 같은 꾸짖는 표현은 사용하지 않는다.
- 색상에만 의존하지 않고 아이콘과 짧은 텍스트를 함께 쓴다.
- 수학 시간 초과는 `시간이 다 되었어요. 정답을 같이 볼까요?`라고 안내하고 정답을 표시한다. 점수상 오답으로 계산하되 결과에 시간 초과 수를 별도로 둔다. 언어에는 시간 초과가 없다.
- 언어 오답은 즉시 반복하지 않고 목적별 복습 구간에서 간격을 둔 뒤 한 번 다시 만날 수 있다.

## 15. 효과음 및 애니메이션

### 15.1 효과음

- 외부 음원 없이 Web Audio API의 짧은 oscillator 톤으로 생성하여 오프라인·라이선스 문제를 피한다.
- 정답음은 부드럽게 올라가는 두 음, 총 180~250ms, 기본 gain 0.2 이하로 한다.
- 오답은 효과음을 기본적으로 내지 않거나 100ms 이하의 매우 약한 중립음을 사용한다. 첫 릴리스 기본은 무음이다.
- AudioContext는 세션 `시작` 같은 사용자 제스처 안에서 resume한다. 실패하면 시각 피드백만 사용한다.
- 효과음 토글 기본값은 켜짐이며 고정된 낮은 음량을 쓴다. 1차 구현에는 볼륨 슬라이더를 넣지 않는다.

### 15.2 애니메이션

- 버튼 누름은 scale `0.98` 정도, 100~150ms 이내다.
- 화면 전환은 opacity/translate 150~220ms 이내다.
- 정답 축하는 500~1,000ms, 최대 1,200ms를 넘지 않는다.
- transform과 opacity만 주로 사용하고 레이아웃을 흔드는 애니메이션을 피한다.
- OS 동작 줄이기가 앱 설정의 애니메이션 켜짐보다 우선한다.

## 16. 결과 화면

완료 세션은 다음 수치를 계산한다.

- `correctCount`: 정답 수
- `incorrectCount`: 선택 오답 수
- `timeoutCount`: 시간 초과 수
- `totalCount`: 완료한 문제 수
- `averageResponseMs`: 각 문제의 `ANSWERING` 시작부터 확정까지 걸린 시간 평균. 피드백·숨김 일시 정지·첫 TTS 재생 시간은 제외
- `accuracy`: 내부 계산용 비율. 아이용 주 표시는 퍼센트보다 `8 / 10` 형식

결과 문구는 점수와 무관하게 노력 중심으로 끝낸다.

- 90~100%: `정말 멋지게 해냈어요!`
- 70~89%: `아주 잘했어요!`
- 40~69%: `차근차근 잘 풀었어요!`
- 0~39%: `끝까지 해낸 게 멋져요!`

별 개수는 점수를 벌처럼 보이게 하지 않도록 장식으로만 사용하고 `별 1개 실패` 같은 비교 표현은 쓰지 않는다. 평균 응답 시간은 초 단위 한 자리 소수로 표시하되 평가 문구를 붙이지 않는다.

## 17. 데이터 구조

아래는 논리 스키마이며 구현 시 TypeScript 타입으로 정의한다.

### 17.1 공통 열거형

- `Subject`: `math | korean | english`
- `Difficulty`: `easy | normal | hard | challenge`
- `QuestionStatus`: `generating | presenting | answering | feedback | advancing`
- `Resolution`: `correct | incorrect | timeout`

### 17.2 생성된 문제

| 필드 | 의미 |
|---|---|
| `id` | 세션 내 유일한 불투명 ID |
| `signature` | 반복 감지용 정규화 서명 |
| `subject`, `mode`, `difficulty` | 출제 맥락 |
| `kind` | `math`, `fill`, `listening` 중 하나 |
| `prompt` | 표시 식 또는 마스킹된 문자열. 듣기는 일반 제목만 포함 |
| `hint` | 언어 채우기 문제의 비정답 힌트 |
| `speech` | 듣기일 때만 `{ text, lang }` |
| `options` | `{ id, label, value }` 배열 |
| `correctOptionId` | 유일한 정답 ID |
| `explanation` | 피드백에서 표시할 정답 문자열 또는 짧은 식 |
| `metadata` | 피연산자, 원본 단어 ID, 마스크 구간 등 테스트·기록 보조 정보 |

듣기 문제의 `speech.text`는 앱 메모리에는 존재하지만 피드백 전에는 보이는 DOM이나 접근성 레이블에 넣지 않는다. 개발 로그에도 전체 문제를 기본 출력하지 않는다.

### 17.3 언어 데이터

- 한국어: `id`, `word`, `difficulty`, `category`, `hintKo`, `emoji?`, `maskRanges`, `distractorChunks?`, `ttsLang`
- 영어: `id`, `word`, `meaningKo`, `difficulty`, `category`, `maskRanges`, `distractorChunks?`, `phonicsSkills`, `ttsLang`
- `maskRanges`는 시작 인덱스와 길이의 목록이다. 데이터 검증에서 범위가 단어 안에 있고 난이도 규칙에 맞는지 확인한다.
- `phonicsSkills`는 `digraph-sh`, `vowel-team-ee`, `silent-e`처럼 철자 학습 단위를 나타내며 현재 정적 단어 데이터에서 자동 계산한다.

### 17.4 답안 기록

- `questionId`, `questionSignature`
- `selectedOptionId` 또는 `null`
- `correctOptionId`
- `resolution`
- `responseMs`
- `answeredAt`

최근 기록에는 전체 보기나 정답 단어를 저장하지 않고 세션 요약만 저장하여 용량과 개인정보 범위를 줄인다.

## 18. 저장 방식

데이터가 작으므로 IndexedDB 대신 `localStorage`를 사용한다.

### 18.1 키

- `numbercal.settings.v1`: 효과음, TTS, TTS 읽기 속도, 애니메이션, 마지막 과목·모드·난이도·세션 길이·테마
- `numbercal.history.v1`: 최근 완료 세션 요약 최대 20개
- `numbercal.language-mastery.v1`: 언어 모드·단어별 시도 수, 연속 정답, 평균 응답 시간, 숙련 단계, 다음 복습 시각
- `numbercal.skill-mastery.v2`: 기술별 독립·도움 정답, 최근 정확도, 힌트율, 확신도, 다음 복습 시각. 처음 읽을 때 v1 낱말 기록을 보존한 채 기술 증거로 이관

설정과 기록 객체 안에도 `schemaVersion: 1`을 둔다. 읽을 때 JSON 파싱·필드 타입·열거값을 검사하고 잘못된 필드만 기본값으로 되돌린다. 완전히 손상된 데이터는 해당 키를 초기화한다.

### 18.2 저장 규칙

- 설정 변경은 즉시 저장한다.
- 완료 세션만 결과 화면 진입 시 한 번 저장한다.
- 언어 답안은 피드백이 확정될 때 개인 식별 정보 없이 단어별 집계만 즉시 저장한다. 숙련 단계는 `new / learning / almost / mastered / review`를 사용한다.
- 진행 중 세션과 개별 답안 상세는 저장하지 않는다. 새로고침 시 홈으로 돌아가며 불완전 세션은 버린다.
- 기록은 최신순, 최대 20개다. 21번째를 추가할 때 가장 오래된 항목을 제거한다.
- `localStorage` 접근은 항상 `try/catch`로 감싼다. 사생활 보호 모드·용량 오류·보안 오류 시 메모리 기본값으로 계속 실행하고 설정 화면에만 `이 기기에는 기록을 저장할 수 없어요.`라고 표시한다.
- 기록 삭제는 완료 기록, 언어 숙련도와 게임 기록을 제거하며 설정은 유지한다.
- 이름, 생년월일, 학교, 위치, 광고 ID 등 개인정보는 수집하지 않는다.

## 19. PWA 설계

### 19.1 Manifest

- `name`: 어린이 학습 놀이터
- `short_name`: 학습 놀이터
- `display`: `standalone`
- `start_url`과 `scope`: 빌드 시 계산된 base path
- `theme_color`와 `background_color`: 실제 앱 초기 배경과 동일
- `orientation`: `any`로 두어 태블릿·landscape를 막지 않음
- 아이콘: 192×192, 512×512 PNG, 512×512 maskable, 180×180 Apple touch icon
- 아이콘의 핵심 도형은 maskable 안전 영역 안에 둔다.

### 19.2 Service Worker

- `vite-plugin-pwa`의 Workbox `generateSW`를 사용한다.
- 빌드 산출 JS/CSS/HTML, 아이콘, manifest, 정적 단어 데이터를 precache한다.
- 외부 CDN, Google Fonts, 원격 이미지, 런타임 API가 없으므로 복잡한 runtime caching은 두지 않는다.
- 문서 탐색은 앱의 `index.html`로 fallback한다. URL 라우터를 쓰지 않으므로 깊은 경로 의존은 없다.
- 캐시 버전은 빌드 해시로 갱신하고 오래된 캐시는 activate 시 제거한다.
- 업데이트는 `prompt` 전략을 사용한다. 활성 세션 중에는 새 service worker가 준비되어도 새로고침하지 않고, 홈 또는 결과 화면에서 `새 버전이 준비됐어요`를 표시한 뒤 사용자 확인으로 적용한다.
- service worker 등록 실패는 앱 사용을 막지 않는다.

### 19.3 오프라인 범위

- 첫 방문과 service worker 설치는 온라인이어야 한다.
- 설치 후 수학·언어 채우기, 설정, 결과 저장은 완전 오프라인이어야 한다.
- 브라우저 TTS 음성은 OS가 원격 음성을 쓰는 경우 오프라인에서 실패할 수 있으며 앱 캐시에 포함되지 않는다. 실패 시 11장의 fallback을 사용한다.
- 빌드 산출물에는 네트워크 요청이 필요한 콘텐츠가 없어야 한다.

## 20. GitHub Pages 배포 방식

### 20.1 base path

- 코드에서 `/assets/...`, `/manifest.webmanifest`, `/sw.js` 같은 루트 절대 경로를 쓰지 않는다.
- Vite `base`와 PWA의 `scope/start_url`은 하나의 `VITE_BASE_PATH` 값에서 파생한다.
- 프로젝트 페이지는 `/<RepositoryName>/`, 사용자·조직 페이지 저장소(`owner.github.io`)는 `/`를 사용한다.
- 앱 내부 asset URL은 Vite import 또는 `import.meta.env.BASE_URL` 기준으로 만든다.

### 20.2 GitHub Actions

워크플로는 다음 단계를 수행한다.

1. checkout
2. 고정된 LTS Node 설치 및 npm cache 사용
3. `npm ci`
4. 저장소 이름으로 base path 계산
5. 타입 검사, 단위 테스트, 프로덕션 빌드
6. `dist`를 Pages artifact로 업로드
7. 공식 Pages deploy action으로 배포

Pages source는 `GitHub Actions`로 설정한다. 워크플로에는 `pages: write`, `id-token: write`, `contents: read` 권한과 Pages environment를 둔다. 배포 검증에서는 실제 `/RepositoryName/` URL에서 새로고침, 아이콘, manifest, service worker scope, 오프라인 재접속을 확인한다.

## 21. 기술 스택과 선택 이유

### 21.1 선택

- React: 화면을 작은 컴포넌트로 나누고 명시적인 상태 기반 렌더링을 하기 충분히 안정적이다.
- TypeScript: 문제·보기·세션 상태의 불변식과 잘못된 전환을 컴파일 단계에서 줄인다.
- Vite: 정적 빌드가 단순하고 base path 설정 및 GitHub Pages 배포가 쉽다.
- `vite-plugin-pwa`: manifest, Workbox precache, update 흐름을 작은 설정으로 관리한다.
- 일반 CSS: CSS Modules 또는 컴포넌트별 CSS 파일과 전역 design token을 사용한다. CSS-in-JS와 대형 UI kit는 사용하지 않는다.
- Vitest + Testing Library: 생성기·reducer·컴포넌트 동작을 같은 TypeScript 환경에서 테스트한다.
- Playwright: 모바일 viewport와 전체 학습 흐름, 오프라인 동작을 검증한다.

### 21.2 의도적으로 제외

- React Router: 화면 수는 있지만 공유해야 할 깊은 URL이 없고 새로고침 복원이 필요하지 않다.
- Redux·Zustand 등 전역 상태 라이브러리: 한 세션의 reducer와 설정 context로 충분하다.
- Tailwind·대형 컴포넌트 라이브러리: 디자인 의존성과 번들·설정 복잡도를 늘리지 않는다.
- 백엔드·Firebase·원격 CMS: 정적·오프라인 목표와 맞지 않는다.
- 외부 웹폰트·아이콘 CDN: 오프라인과 개인정보·로딩 안정성을 위해 사용하지 않는다.

의존성은 구현 시 현재 안정 버전을 선택하고 lockfile을 커밋한다. 버전 범위를 무분별하게 넓히지 않는다.

## 22. 디렉터리 및 파일 구조

권장 구조는 다음과 같다.

```text
NumberCal/
├─ .github/workflows/deploy-pages.yml
├─ public/
│  ├─ icons/
│  │  ├─ icon-192.png
│  │  ├─ icon-512.png
│  │  ├─ icon-maskable-512.png
│  │  └─ apple-touch-icon.png
│  └─ favicon.svg
├─ src/
│  ├─ app/
│  │  ├─ App.tsx
│  │  ├─ appReducer.ts
│  │  └─ appState.ts
│  ├─ components/
│  │  ├─ ChoiceButton.tsx
│  │  ├─ ProgressHeader.tsx
│  │  ├─ Timer.tsx
│  │  ├─ FeedbackOverlay.tsx
│  │  └─ ConfirmDialog.tsx
│  ├─ screens/
│  │  ├─ HomeScreen.tsx
│  │  ├─ ModeScreen.tsx
│  │  ├─ SetupScreen.tsx
│  │  ├─ QuestionScreen.tsx
│  │  ├─ ResultScreen.tsx
│  │  └─ SettingsScreen.tsx
│  ├─ domain/
│  │  ├─ types.ts
│  │  ├─ difficulty.ts
│  │  ├─ questionValidation.ts
│  │  ├─ mathGenerator.ts
│  │  ├─ koreanGenerator.ts
│  │  ├─ englishGenerator.ts
│  │  └─ optionGenerator.ts
│  ├─ data/
│  │  ├─ koreanWords.ts
│  │  ├─ englishWords.ts
│  │  └─ fallbackQuestions.ts
│  ├─ services/
│  │  ├─ speechService.ts
│  │  ├─ soundService.ts
│  │  ├─ storageService.ts
│  │  └─ randomService.ts
│  ├─ hooks/
│  │  ├─ useQuestionTimer.ts
│  │  ├─ useVisibilityPause.ts
│  │  └─ useReducedMotion.ts
│  ├─ styles/
│  │  ├─ tokens.css
│  │  ├─ global.css
│  │  └─ screens.css
│  ├─ test/
│  │  ├─ fixtures/
│  │  └─ setup.ts
│  ├─ main.tsx
│  └─ vite-env.d.ts
├─ tests/e2e/
├─ index.html
├─ vite.config.ts
├─ package.json
├─ tsconfig.json
└─ IMPLEMENTATION_SPEC.md
```

단어 파일이 커지면 난이도 또는 언어별 파일로 나눌 수 있지만 네트워크로 따로 받아오지 않고 빌드에 포함한다. 배럴 파일의 과도한 재수출과 순환 의존은 피한다.

## 23. 핵심 상태 모델

### 23.1 앱 상태

- `screen`: `home | mode | setup | session | result | settings`
- `settings`: 소리·TTS·애니메이션 및 마지막 선택값
- `draftConfig`: 시작 전 선택 중인 과목·모드·난이도·세션 길이·테마
- `session`: 활성 세션 또는 `null`
- `latestResult`: 방금 끝난 결과 또는 `null`

### 23.2 세션 상태

- `id`, `config`, `phase`
- `questionIndex`: 0부터 시작
- `currentQuestion`
- `questionStatus`
- `recentSignatures`: 최대 8개
- `answers`: 확정된 답안 목록
- `streak`
- `deadline`, `remainingMs`, `pausedAt`(수학 세션에서만 시간 값 사용)
- `inputLocked`
- `feedback`: 문구, 종류, 효과 강도

항상 지켜야 할 조건은 다음과 같다.

- `answers.length`는 현재까지 확정된 문제 수와 같다.
- `questionIndex`는 `0 <= index < totalCount`이고 완료 시에만 마지막 이후로 넘어간다.
- `FEEDBACK`이면 현재 question ID에 대한 답안이 정확히 하나 존재한다.
- `ANSWERING`일 때만 `inputLocked`가 false다.
- `COMPLETED` 뒤에는 문제 생성, 타이머, 점수 변경이 일어나지 않는다.
- 설정 변경은 이미 생성된 현재 문제의 보기 수나 정답을 바꾸지 않는다. 세션 중 설정 화면에서는 소리·애니메이션 토글만 즉시 적용하고 난이도·세션 길이·테마는 다음 세션부터 적용한다.

### 23.3 상태 전환 표

| 현재 | 이벤트 | 다음 | 핵심 부수 효과 |
|---|---|---|---|
| 설정 | 시작 | 시작 중 | session ID 생성, 기존 비동기 작업 취소 |
| 생성 중 | 생성 성공 | 제시 중 | question ID와 고정된 보기 저장 |
| 제시 중 | 표시/첫 음성 완료 | 답변 중 | 시작 시각·deadline 설정 |
| 답변 중 | 유효한 선택 | 피드백 | 입력 잠금, 정오 판정, 답안 1건 기록 |
| 답변 중 | deadline 도달 | 피드백 | 입력 잠금, timeout 1건 기록 |
| 피드백 | 지연 완료 | 다음 이동 | 효과 정리 |
| 다음 이동 | 남은 문제 있음 | 생성 중 | index + 1 |
| 다음 이동 | 마지막 문제 완료 | 완료 | 결과 계산·한 번 저장 |
| 활성 상태 | 페이지 숨김 | 숨김 일시정지 | 타이머/TTS 정지 |
| 숨김 일시정지 | 계속하기 | 이전 활성 상태 | 남은 시간으로 재개 |
| 활성 상태 | 나가기 요청 | 확인 일시정지 | 타이머/TTS 정지 |
| 확인 일시정지 | 계속 풀기 | 이전 활성 상태 | 남은 시간으로 재개 |
| 확인 일시정지 | 나가기 확정 | 홈 | 세션 폐기, 비동기 작업 정리 |

## 24. 예외 상황

- 문제 생성 30회 실패: 최근 중복 제한만 완화해 다시 시도하고, 그래도 실패하면 빌드 시 검증된 fallback 문제를 사용한다.
- fallback도 없음: 오류 화면 대신 현재 설정 화면으로 돌아가 `이 단계의 문제를 준비하지 못했어요. 다른 단계를 골라 주세요.`를 표시한다. 콘솔에는 개발 모드에서만 상세 원인을 남긴다.
- 단어 데이터 오류: 빌드·테스트 단계 validator가 중복 ID, 잘못된 마스크, 부족한 보기 후보, 빈 힌트를 실패 처리한다.
- TTS 없음·실패: 11장의 읽기 문제 전환 규칙을 따른다.
- Web Audio 없음·차단: 효과음 없이 계속 진행한다.
- 저장소 읽기·쓰기 실패: 메모리 상태로 계속 진행한다.
- service worker 없음·등록 실패: 온라인 정적 앱으로 계속 동작한다.
- 새 버전 대기: 진행 중 세션에서는 적용·새로고침하지 않는다.
- 새로고침·브라우저 종료: 진행 세션은 복원하지 않고 홈에서 새로 시작한다.
- 브라우저 뒤로 가기: URL 라우터를 쓰지 않으므로 앱 내 뒤로 버튼을 우선한다. `beforeunload` 경고는 모바일에서 신뢰할 수 없으므로 핵심 보호 수단으로 쓰지 않는다.
- 화면 회전·크기 변경: 현재 문제와 보기 순서를 유지하고 레이아웃만 재배치한다.
- 시스템 시간이 바뀜: 문제 타이머는 `Date.now`가 아닌 monotonic clock을 사용하므로 영향을 받지 않는다.
- 긴 단어·큰 수: 버튼 글자가 잘리지 않도록 줄바꿈 또는 글자 크기 한 단계 축소를 허용하되 최소 22px을 유지한다.

## 25. 모바일·iOS·Android 고려사항

- viewport에 `width=device-width, initial-scale=1, viewport-fit=cover`를 사용하되 사용자 확대를 막는 `maximum-scale=1`은 쓰지 않는다.
- 상하좌우 여백에 `env(safe-area-inset-*)`를 더한다.
- 높이는 `100dvh`를 우선하고 미지원 브라우저용 `100vh` fallback을 둔다.
- 작은 스마트폰은 한 열, 태블릿은 보기 2열을 기본으로 한다. landscape에서는 문제와 보기를 좌우 2영역으로 배치할 수 있으나 읽는 순서와 DOM 순서는 유지한다.
- 본문 폭은 태블릿에서 약 760px로 제한하여 버튼이 과도하게 넓어지지 않게 한다.
- hover에 의존하지 않는다. 버튼은 `touch-action: manipulation`을 사용하고 pointer event 하나로 입력 경로를 통일한다.
- 스크롤이 필요하면 전체 화면을 고정하기보다 정상 세로 스크롤을 허용한다. 보기 마지막 항목이 safe area 뒤에 가려지지 않아야 한다.
- iOS standalone용 meta와 Apple touch icon을 제공한다. 설치 유도는 강제 팝업 대신 설정 또는 홈의 작은 안내로 제공한다.
- iOS의 TTS·Web Audio는 사용자 제스처가 필요할 수 있으므로 `시작`과 `다시 듣기` 탭에서 초기화한다.
- Android 뒤로 가기 동작은 가능한 경우 나가기 확인과 연결하되 브라우저 기본 탐색을 무리하게 가로채지 않는다.
- 키보드 없이 모든 기능을 쓸 수 있어야 하며 가상 키보드를 여는 입력 필드는 두지 않는다.

## 26. 접근성

- WCAG 2.2 AA 수준을 목표로 한다.
- 기본 본문 18px 이상, 문제 32px 이상, 보기 24px 이상을 권장한다. 작은 화면에서도 보기 22px 아래로 내리지 않는다.
- 일반 텍스트 대비 4.5:1, 큰 텍스트 3:1 이상을 지킨다.
- 정답·오답·선택·비활성 상태를 색상만으로 구분하지 않고 아이콘, 테두리, 텍스트를 함께 사용한다.
- 모든 버튼은 의미 있는 accessible name과 보이는 focus indicator를 가진다.
- 키보드 사용자는 Tab으로 이동하고 Enter 또는 Space로 선택할 수 있다. 숫자키 바로가기는 필수가 아니다.
- 문제 시작 시 문제 제목으로 focus를 옮기고, 피드백은 방해가 적은 `aria-live="polite"` 영역에서 한 번만 알린다.
- 타이머는 매초 스크린 리더에 알리지 않는다. 10초와 5초 남음만 polite 안내한다.
- 애니메이션 감소 설정을 존중하고 깜빡임은 초당 3회 미만으로 제한한다.
- TTS 듣기 문제는 청각 접근성이 제한되므로 TTS를 끈 사용자는 같은 학습 목표의 채우기 모드로 전환할 수 있어야 한다.
- 이모지는 장식이면 `aria-hidden`, 단서이면 동등한 텍스트 힌트를 함께 제공한다.
- 모달은 focus trap, ESC 닫기, 닫힌 뒤 원래 버튼으로 focus 복귀를 지원한다.

## 27. 테스트 항목

### 27.1 단위 테스트

- 각 수학 난이도·연산별 10,000개 생성에서 범위, 비음수 뺄셈, 정답 계산을 검증
- 모든 문제의 보기 수, 중복 없음, 정답 정확히 하나를 검증
- 교환법칙 서명과 최근 8문제 중복 방지 검증
- 수학 오답이 정답과 같지 않고 난이도별 허용 범위에 드는지 검증
- 한국어 NFC, 완성형 음절 마스킹, mask range 유효성 검증
- 영어 소문자·길이·힌트·마스크·모호한 오답 필터 검증
- 언어 데이터 난이도별 최소 개수와 listening 보기 구성 가능성 검증
- seed 난수로 동일 입력이 동일 문제 시퀀스를 만드는지 검증
- 저장 데이터 마이그레이션, 손상 JSON, storage 예외 검증
- 결과 수치와 평균 응답 시간 계산 검증

### 27.2 상태 전환 테스트

- 선택 직후 두 번 더 탭해도 답안과 점수가 한 번만 기록됨
- 선택과 timeout을 같은 시점에 발생시켜도 resolution이 하나뿐임
- 이전 question ID의 timeout/TTS/feedback 콜백이 새 문제에 영향 없음
- React Strict Mode에서 결과 저장·효과음·답안 추가가 중복되지 않음
- 숨김·복귀에서 타이머가 멈추고 응답 시간에서 제외됨
- 피드백 중 나가기와 새 세션 시작 시 모든 비동기 작업이 정리됨
- 마지막 문제는 다음 문제를 만들지 않고 결과를 정확히 한 번 저장함

### 27.3 컴포넌트·접근성 테스트

- 난이도별 보기 개수와 비활성 상태 표시
- 정답·오답·timeout 문구와 정답 공개
- 키보드 탐색, focus 복귀, aria-live 중복 안내 없음
- 동작 줄이기에서 confetti와 이동 애니메이션 없음
- 320px 폭에서 텍스트·버튼 겹침 없음

### 27.4 E2E 및 실제 기기

- 각 과목에서 `홈 → 설정 → 5문제 → 결과 → 한 번 더` 전체 흐름
- iPhone SE급 작은 viewport, 최신 iPhone, iPad, Android phone/tablet, desktop viewport
- portrait와 landscape 회전 후 문제·선택 상태 보존
- 실제 iOS Safari와 Android Chrome에서 첫 TTS, 다시 듣기, AudioContext 확인
- GitHub Pages 하위 경로에서 asset·manifest·SW 404 없음
- 온라인 첫 로드 뒤 네트워크를 끄고 새로고침하여 채우기 세션 완료 가능
- 업데이트 대기 중 활성 세션이 강제 새로고침되지 않음
- Lighthouse PWA·접근성 점검. 점수 자체보다 실패 항목을 수정 기준으로 사용

## 28. 구현 우선순위

### 1단계: 기반과 수학 세로 슬라이스

- Vite/React/TypeScript 구성, 전역 토큰과 반응형 레이아웃
- 공통 타입, 난이도 설정, reducer 상태 모델
- 홈·모드·설정·문제·결과 화면의 최소 흐름
- 덧셈 생성기와 보기 선택, 정오답 피드백, 5문제 기본·15문제 선택 결과
- 연타·timer 경쟁 조건 테스트

완료 기준: 작은 모바일 viewport에서 덧셈 5문제와 선택형 15문제를 오류와 중복 기록 없이 끝낼 수 있다.

### 2단계: 수학 완성과 피드백 품질

- 뺄셈·곱셈·사칙연산 및 4단계 규칙
- 오답 후보 품질, 반복 방지, 30초 고정 타이머
- 칭찬 문구, streak, Web Audio 효과음, reduced motion
- 수학 property 테스트

### 3단계: 한국어·영어 채우기

- 검증된 단어 데이터와 build-time validator
- 한국어 음절 묶음 및 영어 철자 묶음 생성기
- 힌트와 모호성 방지, 언어별 선택지 fallback
- 데이터·생성기 테스트

### 4단계: 듣기와 기기 fallback

- voice 선택, 첫 발화, 다시 듣기, cancel, 오류 처리
- 언어 무제한 시간과 다시 듣기 규칙
- 미지원·오프라인 실패 시 채우기 문제 전환
- 실제 iOS Safari·Android Chrome 확인

### 5단계: 저장·PWA·Pages

- 버전형 localStorage와 최근 20개 기록
- manifest, 아이콘, service worker, update prompt
- 동적 base path의 GitHub Actions Pages 배포
- 하위 경로·오프라인 E2E

### 6단계: 출시 전 다듬기

- 접근성, focus, safe area, landscape, 320px 폭 검증
- 전체 E2E와 실제 기기 회귀 테스트
- 문구·색 대비·애니메이션 피로도 점검
- 사용하지 않는 의존성·네트워크 요청 제거

## 최종 완료 조건

다음 조건이 모두 충족되어야 1차 버전을 완료로 본다.

- 세 과목의 모든 명시된 모드와 5개 난이도가 동작한다.
- 모든 문제는 난이도에 맞는 2~4개 선택지를 가지며 정답이 정확히 하나다.
- 답 선택·timeout·연타 경합에서도 문제당 결과가 한 번만 기록된다.
- 정답은 짧고 즐거운 피드백, 오답은 비난 없는 피드백과 정답 공개를 제공한다.
- TTS 미지원·실패, 저장 실패, service worker 실패에서도 앱의 읽기 학습은 중단되지 않는다.
- 작은 스마트폰부터 태블릿·landscape까지 핵심 버튼과 보기가 가려지지 않는다.
- GitHub Pages의 `/RepositoryName/` 하위 경로에서 직접 로드와 오프라인 재접속이 성공한다.
- 외부 서버·원격 콘텐츠·유료 API 없이 설치 가능한 PWA로 동작한다.
